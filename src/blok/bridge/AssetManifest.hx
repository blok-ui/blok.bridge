package blok.bridge;

// @todo: this will be a way to add links and stuff
// that can be output in the HTML.
//
// This should also save a list of all the files we have and
// output it in a `manifest.json` file next to the compiled app.
class AssetManifest {}
